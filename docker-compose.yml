version: "3.9"

services:
  server:
    build: ./serveur
    container_name: serveur_taches
    ports:
      - "5000:5000"
    restart: unless-stopped

  client:
    build: ./client
    container_name: client_taches
    depends_on:
      - server
    stdin_open: true
    tty: true

